## What's in this PR

<!-- One or two sentences: new essay, edit to an existing piece, new contributor page, etc. -->

## Checklist

- [ ] Frontmatter fields are filled in (see `src/content.config.ts` for the schema)
- [ ] `status` is set correctly (`draft` / `in-review` / `scheduled` / `published`)
- [ ] Images have alt text
- [ ] I ran `npm run build` locally (or checked the PR preview) and it succeeded
